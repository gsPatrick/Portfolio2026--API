{
  "name": "portfolio-tracking-api",
  "version": "1.0.0",
  "private": true,
  "description": "API de rastreamento (analytics) do portfólio Patrick.Developer",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "migrate": "sequelize-cli db:migrate",
    "migrate:undo": "sequelize-cli db:migrate:undo",
    "smoke:test": "node scripts/smoke.js",
    "seed:demo": "node scripts/seed-demo.js",
    "db:limpar": "node scripts/limpar.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.3"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.2"
  }
}
